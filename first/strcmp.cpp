#include <iostream>
#include "strcmp.h"
bool StrCmp(const char* c){
    if (c == nullptr){
        throw 
    }
    try{

    }catch(...){

    }
}

int StrCmp::strLength(const char* c1st){
    // c1st为空时直接返回长度为0
    if (c1st == nullptr){
        return 0;
    }
    int str_length = 0;
    for (const char* p; *p != '\0'; p++){

    }
}